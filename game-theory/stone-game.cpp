class Solution {
public:
    bool stoneGame(vector<int>& piles) {
     return true ;   // its optimaization problem , so both will make move to gain more numebr , so whoever will make first move that will be the winner   
    }
};